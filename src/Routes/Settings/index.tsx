import * as Styled from './styles';

export type SettingsProps = { title?: string };
export const Settings = ({ title }: SettingsProps) => {
  return (
    <Styled.Wrapper>
      <h1>Teste</h1>
    </Styled.Wrapper>
  );
};
